import { useState } from "react";
 
const App = () => {
    const arr = ['janu', 'mercy', 'bobby', 'nancy'];
 
    return (
        <>
            <h1>React App</h1>
            <table border="2" width="200px">
             
                    {arr.map((x) => (
                        <tr>
                            <td>{x}</td>
                        </tr>
                    ))}
              
            </table>
        </>
    );
  }
 
export default App;
 